version: "2"
linters:
  default: all
  disable:
    - gomodguard
    - noinlineerr
    - paralleltest
    - depguard
    - exhaustruct
    - godoclint
    - testpackage
    - wsl
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - err113
          - noctx
          - varnamelen
formatters:
  enable:
    - gci
    - gofumpt
    - goimports
    - golines
