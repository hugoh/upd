package pkg

const (
	// DNS protocol constant.
	DNS string = "dns"
	// HTTP protocol constant.
	HTTP string = "http"
	// HTTPS protocol constant.
	HTTPS string = "https"
	// TCP protocol constant.
	TCP string = "tcp"
)
